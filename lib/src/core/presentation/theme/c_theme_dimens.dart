import 'package:flutter/material.dart';

abstract final class CThemeDimens {
  // radius
  static final buttonRadius = BorderRadius.circular(10.0);
  static final contentBoxRadius = BorderRadius.circular(10.0);

  // padding
  static const screenPadding = EdgeInsets.all(16.0);

  // dimens
  static const iconSize = 30.0;
}
