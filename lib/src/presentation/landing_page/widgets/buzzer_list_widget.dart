import 'package:flutter/material.dart';

class BuzzerListWidget extends StatelessWidget {
  const BuzzerListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
